<Window x:Class="militreg_lite.UI.ImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:militreg_lite.UI"
        mc:Ignorable="d"
        Title="Імпорт" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label Content="Оберіть ТЦК" Margin="20,0,0,0"></Label>
        <ComboBox x:Name="cbRtck" ItemsSource="{Binding Rtcks}" DisplayMemberPath="Name" Height="30" Margin="20,0,20,0"></ComboBox>
        <Grid Grid.Row="1" Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnCancel" Content="Відмінити" Click="btnCancel_Click"></Button>
            <Button x:Name="btnImport" Grid.Column="1" Content="Імпорт" Click="btnImport_Click"></Button>
        </Grid>
    </Grid>
</Window>
